@model Hospital.UI.Models.NurseViewModel

<div class="col-12 col-lg-9 order-lg-0 mt-1 bg-light rounded">
    <h2 class="text-center">Nurse:</h2>
    <table class="table">
        <tr>
            <th scope="col">LastName</th>
            <th scope="col">First name</th>
        </tr>
        <tr>
            <td>@Model.LastName</td>
            <td>@Model.FirstName</td>
        </tr>
    </table>
    @Html.ActionLink("Back", "Index", "Nurse", null, new { @class = "btn btn-secondary  mb-1" })
</div>